// Mengimpor pustaka `moment`
// `moment` adalah pustaka JavaScript yang digunakan untuk memanipulasi dan format tanggal serta waktu.
import moment from "moment";

// Menampilkan tanggal dan waktu saat ini dengan format default
// `moment()` mengembalikan objek `moment` yang mewakili tanggal dan waktu saat ini
// `format()` tanpa argumen mengembalikan tanggal dan waktu saat ini dalam format default ISO 8601
console.log(moment().format());
