// Menjadwalkan eksekusi fungsi dengan delay menggunakan 'setTimeout'
// Fungsi ini akan dipanggil setelah 2000 milidetik (2 detik) dari saat 'setTimeout' dipanggil
setTimeout(() => {
   // Mencetak pesan "Hello Globals" ke konsol setelah delay
   console.info("Hello Globals");
}, 2000);
