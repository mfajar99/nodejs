/**
 * Fungsi `getProductById` bertujuan untuk mengambil produk berdasarkan ID produk.
 * Saat ini, fungsi ini belum diimplementasikan dan akan melemparkan error jika dipanggil.
 * 
 * @param {number} id - ID dari produk yang ingin diambil
 * @throws Error jika fungsi dipanggil sebelum diimplementasikan
 * @returns {void} - Fungsi ini belum mengembalikan apapun karena belum diimplementasikan
 */
export const getProductById = (id) => {
   // select * from products where id = ${id}
   throw new Error("Not Implemented"); // Menunjukkan fungsi belum diimplementasikan
};

/**
 * Fungsi `getAllProducts` bertujuan untuk mengambil semua produk dari tabel produk.
 * Saat ini, fungsi ini belum diimplementasikan dan akan melemparkan error jika dipanggil.
 * 
 * @throws Error jika fungsi dipanggil sebelum diimplementasikan
 * @returns {void} - Fungsi ini belum mengembalikan apapun karena belum diimplementasikan
 */
export const getAllProducts = () => {
   // select * from products
   throw new Error("Not Implemented"); // Menunjukkan fungsi belum diimplementasikan
};
