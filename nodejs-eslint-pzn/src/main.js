// Mendefinisikan dan mengekspor fungsi sayHello.
export const sayHello = (name) => {
   // Membuat pesan sapaan dengan menggunakan template literal.
   const message = `Hello ${name}`;

   // Menampilkan string literal "message" ke konsol, bukan nilai variabel message.
   console.log(`message`);
};
